# =============================================================================
# Python 3.10+ required.
#
# Install:  pip install -r requirements.txt
#
# PyTorch note: the wheel below is CPU-only by default. For a local NVIDIA GPU,
# install the CUDA build FIRST, then run this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Check your CUDA version with `nvidia-smi` and pick the matching index URL
# from https://pytorch.org/get-started/locally/
# =============================================================================

# --- core numerics -----------------------------------------------------------
numpy>=1.24,<3.0
scipy>=1.10
pandas>=2.0

# --- classical ML ------------------------------------------------------------
# StratifiedGroupKFold needs >=0.24; we rely on modern sklearn behaviour.
scikit-learn>=1.3
joblib>=1.3

# --- audio and signal processing ---------------------------------------------
librosa>=0.10          # MFCC, mel filterbank, STFT, delta features
soundfile>=0.12        # fast wav I/O (preferred backend in src/data/loader.py)
PyWavelets>=1.4        # wavelet packet transform for the PWP features

# --- deep learning -----------------------------------------------------------
torch>=2.0             # CNN; install the CUDA build separately for GPU

# --- explainability ----------------------------------------------------------
shap>=0.44             # TreeSHAP and KernelSHAP

# --- plotting ----------------------------------------------------------------
matplotlib>=3.7
seaborn>=0.12          # only for a handful of palettes

# --- config and utilities ----------------------------------------------------
PyYAML>=6.0
tqdm>=4.65

# --- testing (optional but recommended) --------------------------------------
pytest>=7.4
